  <!-- 查询记录 -->
  <view class="guide">
    <view wx:if="{{result_array.length > 1}}" wx:for="{{result_array}}">
      <view class="userinfo"><text bindtap="toChat" data-roomid="{{item._id}}">{{item._id}}</text></view>
    </view>
  </view>